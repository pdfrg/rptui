# problems or improvements of current features
# roadmap.txt is for major new features or changes

sometimes on entry to visualizer, gets stuck at loading message
	unclear why this happens
	usually if you cycle all the way back around through the views, next time it works OK
	added logging
		during initial tests I had no failures, but we didn't "fix" anything
		on repeated testing visualizer is now "just working" without failure to load
		next time it fails to load, save log and submit for analysis
		saved session as session-ses_294e-vis-debug.md
			load on next attempt

smart terminal size detection
	important for artist thumbs
		if too short a window, image renders over footer, messing up display
		if no room, rather than show image, print message "Increase terminal height and cycle views to view image."
		should be able to detect per image
	recommended min size for large/default 32x113 (rows x columns)
		still not enough height for most artist thumbs, but everything else will work
		height 37+ advised for artist thumbs (some have very tall aspect ratios)
	users can check with "stty size"
	min for medium: 21 113
	min for compact: 18 36
	min for narrow: 35 36
	example hyprland bindings in DOCUMENTATION.md take this into account
	medium and large will work with more narrow terminal, but some keybindings won't be seen in footer
	
add a bunch of files to .gitignore
	will these automatically be removed/not shown on github, even though they are there now?
	
