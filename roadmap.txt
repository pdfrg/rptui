# fixes.txt is for bugs, minor refinements to existing features

crossfade on station changes, jukebox mode
	pseudo-crossfade (vol down, vol up)
	apparently mpv doesn't support real crossfade?
	others have used 2 mpv instances to create real crossfade effect
	mpv --lavfi-complex='[aid1] [aid2] <filter> [ao]'
	says to check ffmpeg libavfilter docs (see crossfade.txt for snippet)
	not sure it is possible to crossfade on demand (likely from middle of file)
		may need to just do pseudo
	jukebox mode
		transitions more predictable
		since audio files are from a radio mix, they may already be "pre-crossfaded"
			however, since we aren't playing back in the same order, they won't match
			some type of crossfade should make this less apparent
		configurable pseudo-crossfade already implemented

cache for offline listening (good for flights, etc) -- DONE
	different than favorites, though jukebox mode can run offline
	another option for offline listening -- normal radio content downloaded in advance
	launch from cli -- DONE
		e.g. rptui --cache 2h
		saves to ~/.cache/rptui/offline
		would have to run for 2h then close
		maybe print time remaining to the terminal (show progress/time remaining) -- DONE (text progress)
		when you run rptui without connection, detect lack of internet, presence of offline cache
			ask user if they want to run in offline mode
		would need a cache manager -- DONE
			could just add to manage modal with faves and blocklist items -- DONE (third "Offline" tab)
		give hint as to how much disk space used per hour for each quality -- DONE
			AAC 64k 128k 320k should be able to calculate easily -- DONE
			do FLAC bitrates vary?  can use examples in ~/.cache/rptui/favorites -- DONE (calculated from actual .flac files)
				ZZ Top song is 4:37, Pearl Jam 4:15
		should be able to save and play multiple saves -- DONE
			grab a 2h block of main mix, and a 2h block of rock mix
		if no options, default to config file settings for station and bitrate -- DONE
			but show and ask for confirmation -- DONE
			can give options for station and bitrate -- DONE
				--cache 2h main 320
				--cache 3.5h rock flac
				--cache 1h mellow 128
	playback -- DONE
		sequential playback (RP broadcast order, no shuffle)
		no crossfade (natural RP transitions preserved)
		blocklist: shared with normal mode, blocked songs skipped during playback
		favoriting: works normally, copies from cache to favorites
		album art: displayed from cached URLs
		lyrics/artist views: show "not available in offline mode"
		UI indicator: [OFFLINE: cache_name] in footer
		always starts from beginning (no resume position)
		--offline flag: trusted regardless of internet connectivity
	cache recording behavior -- DONE
		headless mode with text progress
		queuing: multiple --cache flags run sequentially
		Ctrl+C: prompts confirmation, deletes partial cache if confirmed
		not a hard limit: completes the song that crosses the time boundary
		direct download: saves gapless_url files as-is (AAC/FLAC)
		disk space check: warns if <1GB remaining, refuses if would fill disk
	cache management -- DONE
		--list-caches: shows all caches with details
		--delete-cache: deletes specific cache with confirmation
		third "Offline" tab in manage modal
		auto-generated cache names: {station}_{bitrate}_{YYYYMMDD}_{HHMM}
		manual removal only (no auto-expire)

implement command line switches (override config file)
	already  have --lastfm-auth and -j --jukebox
	size small (no album art, no playlist -- like toggle "v" to off) -s small or --size small
	size medium (no playlist)
	size large (default, show all)
	size narrow (like medium, but albumart on top, like mobile apps)
	if run from launcher, can we open terminal of just that size?
	if run from within terminal, no resize
	I can do manual tests, changing display and manually resizing terminal, then check terminal size

rptui --help
	show all options with explanations

RP has different API endpoint to get all stations
	see rp_notes.txt
	have 2 stations we don't have
	have rptui check and report new, missing, misconfigured

-------features below require RP login---------
not to implement (if ever) until later, all roadmap items above complete
refer to rp_notes.txt

RP login with /auth

RP comments support (show, not create)

RP song ratings
	show user rating
	submit user rating

RP favorites
	user configurable 7-10 on RP website
	channel 99 = My Paradise (favorites station)
	
