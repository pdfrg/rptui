# fixes.txt is for bugs, minor refinements to existing features

crossfade on station changes, jukebox mode
	pseudo-crossfade (vol down, vol up)
	apparently mpv doesn't support real crossfade?
	others have used 2 mpv instances to create real crossfade effect
	mpv --lavfi-complex='[aid1] [aid2] <filter> [ao]'
	says to check ffmpeg libavfilter docs (see crossfade.txt for snippet)
	not sure it is possible to crossfade on demand (likely from middle of file)
		may need to just do pseudo
	jukebox mode
		transitions more predictable
		since audio files are from a radio mix, they may already be "pre-crossfaded"
			however, since we aren't playing back in the same order, they won't match
			some type of crossfade should make this less apparent

search and play/enqueue favorites directly
	play is working through options modal
	add enqueue option
	add search to manage modal

desktop notifications
	notify-send?
	albumart thumbnail on left
	text on right
		rptui - Radio Paradise
		<station name> (full, not abbreviated)
		<song>
		<artist>
		<album (year)>
		same colors as in app, if that's possible with notify-send or whatever	
	at least 3 seconds, maybe 5, if that's also configurable	

cache for offline listening (good for flights, etc)
	different than favorites, though jukebox mode can run offline (I think)
	possibly just launch from cli without running full tui
		e.g. rptui --cache 2h
		saves to ~/.cache/rptui/offline
		would have to run for 2h then close
		maybe print time remaining to the terminal
		when you run rptui without connection, detect lack of internet, presence of offline cache
			ask user if they want to run in offline mode
		would need a cache manager
			could just add to manage modal with faves and blocklist items
		give hint as to how much disk space used per hour for each quality

implement command line switches (override config file)
	already  have --lastfm-auth
	size small (no album art, no playlist -- like toggle "v" to off) -s s or -s small
	size medium (no playlist) -s m or -s medium
	size large (default, show all) -s l or -s large
	size narrow (like medium, but albumart on top, like mobile apps)
	if run from launcher, can we open terminal of just that size?
	if run from within terminal, no resize

favorites "jukebox" mode -- queue all faves random and play, at end repeat all
	--jukebox or -j
	no API queries
	jukebox_min_faves = 50 (setting in config file, not editable from app)
	also add "j" hotkey for "Juke"
		clears playlist, enters jukebox mode just like from command line
	need to decide how to handle playlist
		add all to playlist, but would then have to scroll
		add a given number of songs, then when near end, add more, prune as needed, similar to normal mode
	would need to show status jukebox mode or similar. is there a jukebox emoji?

copy current artist - album (year) - song to clipboard
	hotkey ideas?
		"y" to yank
		"c" to copy
		
